# shotgate
- PySceneDetect shot/scene detection with YAML config, QC gate, CSV/JSON outputs, and optional scene-start thumbnails.
- Video shot detection pipeline: YAML-driven, QC-filtered, exports CSV/JSON + scene thumbnails.
- YAML-configured shot detection + quality gate for videos (PySceneDetect), exporting CSV/JSON and thumbnails.